# Known compliance failures - features the IMAP backend cannot offer without
# server-side support, or v0.1 gaps that the README already calls out as
# "Not done yet".
#
# Each line is one upstream test ID (see vendor/jmap-test-suite/src/tests/**).
# `npm run test:compliance` allow-lists these: required failures with IDs in
# this list don't fail the build, but they are still reported.
#
# Anything failing OUTSIDE this list is either a real bug or a missing feature
# that's not yet documented as a known limitation.

# --- Thread/changes for the destroy case ------------------------------------
# Email-level destroy entries lift to thread-level changes via the index, but
# when the last email of a thread is destroyed the index has no live record
# of the thread to look up — we'd need to snapshot thread membership at log
# time. The new-email and update paths work; only the destroy-of-last-member
# case still falls through.
thread/changes-after-email-destroy

# --- VacationResponse -------------------------------------------------------
# Vacation requires ManageSieve, and the 1blu provider entry has sieve: null.
# These pass on backends that have a Sieve endpoint configured.
vacation/get-singleton
vacation/get-singleton-null-ids
vacation/get-singleton-properties
vacation/get-not-found-invalid-id
vacation/set-enable-vacation
vacation/set-disable-vacation
vacation/set-dates
vacation/set-html-body
vacation/set-cannot-create
vacation/set-cannot-destroy

# --- Multi-mailbox membership ----------------------------------------------
# README: messages live in exactly one IMAP folder. Tests that depend on
# multi-mailbox seeds or assert a stable id across mailbox moves fail here.
# (The id encodes mailboxIdx+UID, so an IMAP MOVE produces a new id — JMAP
# expects the id to remain stable.)
email/get-mailbox-ids
email/set-update-move-mailbox
email/set-update-add-mailbox
email/set-update-remove-mailbox
email/set-destroy-removes-from-all-mailboxes
